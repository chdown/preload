name: preload
description: A powerful Flutter preloading framework for short video applications with intelligent memory management and sliding window algorithm.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

# 示例应用配置
example:
  name: preload_example
  description: A simple crevalue demonstrating the preload package usage
